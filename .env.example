# Frontend Environment Variables

# VAPID Public Key for Push Notifications
# Get this from running `npx web-push generate-vapid-keys` in the server folder
# This MUST match the VAPID_PUBLIC_KEY in the server's .env
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
